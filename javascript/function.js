const greet = function(name, thanks) {
    console.log(`Happy Birthday ${name}. Many many happy returns of the day. ${thanks}!!`)
    return 1;
}

let name = "SkillF";
let name2 = 'Rohan';

let val = greet(name, "Thanks a lot");
console.log(val);

a = ['Fruit', 'Vegetable', 'Furniture'];
a.forEach(function(element, index, array) {
    console.log(element, index);
})
console.clear();

let ab = window;
console.log(ab);
// window.alert('Hello Pramod')
// c = prompt("Enter your name : ");
// c = confirm("Are you sure you want to delete this page?");

console.log(c);